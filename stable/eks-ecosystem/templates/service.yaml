apiVersion: v1
kind: Service
metadata:
  name: {{ include "eks-ecosystem.fullname" . }}
  labels:
    {{- include "eks-ecosystem.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eks-ecosystem.selectorLabels" . | nindent 4 }}
